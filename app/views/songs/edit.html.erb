<%= form_for @song do |f| %>
  <div id = "error_explanation">
  <% if @song.errors.any? %>
    <h2>
      <%= pluralize(@song.errors.count, "error") %>
      prohibited this post from being saved
    </h2>

    <ul>
      <% @post.errors.full_message.each do |m| %>
        <li><%= m %><li>
      <% end %>
    </ul>
  <% end %>
  </div>

  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :released %>
  <%= f.check_box :released %>
  <%= f.label :release_year %>
  <%= f.text_field :release_year %>
  <%= f.label :genre %>
  <%= f.text_field :genre%>
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>
  <%= f.submit %>
<% end %>
